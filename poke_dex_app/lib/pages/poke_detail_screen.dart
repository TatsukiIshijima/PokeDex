import 'package:flutter/cupertino.dart';

class PokeDetailScreen extends StatelessWidget {
  const PokeDetailScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('PokeDetailScreen'),
    );
  }
}
